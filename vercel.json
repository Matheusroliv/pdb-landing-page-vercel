{
  "installCommand": "yarn install --ignore-platform",
  "buildCommand": "yarn run build",

  "outputDirectory": "dist/portal-landing-page/browser",

  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
